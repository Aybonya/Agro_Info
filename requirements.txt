# torch/torchvision ставятся отдельно, CPU-сборкой (см. README) — иначе pip
# на Windows по умолчанию тянет CUDA-сборку torch (~2.5 ГБ) и install идёт очень долго
ultralytics==8.3.40
opencv-python==4.10.0.84
paddlepaddle==3.3.1
paddleocr==3.7.0
open_clip_torch==2.29.0
pandas==2.2.3
pillow==11.0.0
numpy==1.26.4
